apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Infrastructure
  - cert-manager.yaml
  - external-secrets.yaml
  - monitoring.yaml
  - keel.yaml
  - argocd.yaml

  # Applications
  - n8n.yaml
  # - supabase.yaml  # TODO: Helm repo unavailable, Supabase managed manually for now
