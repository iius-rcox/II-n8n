apiVersion: v1
kind: Service
metadata:
  name: n8n
  namespace: n8n-prod
  labels:
    app: n8n
    environment: production
spec:
  type: ClusterIP
  ports:
  - port: 5678
    targetPort: 5678
    protocol: TCP
    name: http
  selector:
    app: n8n
