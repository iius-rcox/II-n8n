apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: n8n-prod

resources:
  - namespace.yaml
  - storage/
  - rbac/
  - secrets/
  - network/
  - backup/

commonLabels:
  app: n8n
  environment: production
